/*
 * Luciano Crocco 1ºF
 * Ejercicio 10-2
 * Crear las siguientes estructuras en c, y definir la manera mas optima de relacionarlas.
	A) Producto-Proveedor (Donde el producto solo puede tener un único proveedor)
	B) Dueño-Mascota (Donde la mascota solo puede tener un único dueño)
	C) Alumno-Localidad (Donde un alumno solo vive en una localidad)
 *
 */

#include <stdlib.h>
#include <stdio.h>

int main (void){

	setbuf(stdout,NULL);

	return EXIT_SUCCESS;
}

